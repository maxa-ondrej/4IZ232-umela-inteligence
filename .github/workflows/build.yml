name: Build

on:
  push:
    branches:
      - main

jobs:
  protege:
    name: Protege
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PROJECT: protege
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate UML Diagrams
        uses: ./.github/actions/plantuml
        with:
          files: ${{ env.PROJECT }}/analyza/*.puml
          output-dir: ${{ env.PROJECT }}/analyza

      - name: Generate Referat
        uses: ./.github/actions/xelatex
        with:
          file: referat.tex
          output-dir: ${{ env.PROJECT }}

      - name: Generate Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.PROJECT }}
          path: |
            ${{ env.PROJECT }}/**/*.png
            ${{ env.PROJECT }}/**/*.jpg
            ${{ env.PROJECT }}/**/*.txt
            ${{ env.PROJECT }}/**/*.pdf
            ${{ env.PROJECT }}/**/*.owl
            ${{ env.PROJECT }}/**/*.rdf
